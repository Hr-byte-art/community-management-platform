# 社区治理系统扩展规划（可续接版）

更新时间：2026-03-21  
项目路径：`e:\zsy\biyesheji\biyesheji`

## 0. 接力说明（优先阅读）

下次继续开发时，**优先阅读本节、第 1 节、第 3 节、第 4 节、第 12 节、第 14 节、第 15 节**，即可快速接力，不必通篇翻历史记录。

### 0.1 当前真实状态
- 已完成：
  - Sprint-1：积分中心、消息中心
  - Sprint-2：工单超时治理、超时看板、首页超时统计
  - P1：服务评价闭环、自动派单规则
  - P2：RBAC 菜单/按钮/数据范围权限
  - 工单页面超时治理前端收口
  - 公告订阅设置与发布后站内消息推送
  - 派单规则预览前端接入
  - 公告统计前端接入
  - 活动/流动人口导出入口补齐
  - 消息中心详情弹窗
  - 首页与工单超时看板图表细化
  - `API_DOC.md`、`README.md`、`环境配置指南.md`、`community_service.sql` 同步完成
- 当前仍未完成：
  - 当前无阻塞性未完成项
  - 后续可直接进入“最终展示优化 / 毕设答辩准备 / 新功能扩展”

### 0.2 下一步首选任务
1. 在真实数据库执行总脚本 `houduan/src/main/resources/sql/community_service.sql`
2. 启动前后端，按真实账号做一轮人工回归
3. 输出回归问题清单
4. 若无阻塞，再进入“菜单/页面目录按业务域整理”

### 0.3 下一位模型建议第一条指令
`请先按扩展规划第 0 节与第 3 节执行真实环境回归验收：基于 community_service.sql 重建数据库，逐页检查登录、权限、工单、消息、评价、公告订阅和统计看板，并输出问题清单与修复建议。`

## 1. 当前阶段结论

当前项目状态已经推进到：**Sprint-1 完成、Sprint-2 完成、P1 完成、P2 完成**。

当前主要工作重心已经不再是“基础能力补齐”，而是进入 **功能收口 + 文档同步 + 真实环境回归**。前面各轮会话中的数据库执行、前端联调、首页看板、服务评价闭环、自动派单、RBAC 权限细化都已完成代码落地，并已经通过编译、测试或构建验证。

## 2. 已实现进度（按计划核对）

### 2.1 积分系统（Sprint-1）
- [x] 表结构：`points_account`、`points_record`
- [x] 后端代码：Entity / Mapper / Service / Controller
- [x] 接口：`/points/my`、`/points/records`、`/points/rank`
- [x] 业务接入：活动签到成功后发放积分
- [x] 防重策略：同一业务事件唯一约束防重复发分

### 2.2 消息中心（Sprint-1）
- [x] 表结构：`message_notice`
- [x] 后端代码：Entity / Mapper / Service / Controller
- [x] 用户接口：
  - `GET /message/my`
  - `GET /message/unread/count`
  - `PUT /message/{id}/read`
  - `PUT /message/read-all`
- [x] 管理员接口：`GET /message/admin/list`

### 2.3 消息触发点（已接入）
- [x] 工单处理完成（status=2）后通知提单人
- [x] 活动报名成功后发送消息
- [x] 活动取消报名后发送消息
- [x] 志愿者审核结果（通过/拒绝）发送消息

### 2.4 编译验证
- [x] 已通过：`mvn -DskipTests -Dmaven.resources.skip=true compile`

## 3. 当前未实现项

### 3.1 当前剩余待办
- [ ] 当前无阻塞性未完成项，可直接进入展示优化阶段

### 3.2 可选增强项（非阻塞）
- [x] 消息中心“查看详情”体验增强
- [x] 统计看板图表与数据展示进一步细化
- [x] 将新能力的菜单/页面目录继续按业务域重构整理

## 4. 建议下一步执行顺序

1. 先做 **真实环境回归验收**，确认数据库、权限、消息、评价、公告订阅等能力完整可用
2. 再做 **统计看板细化**，提升答辩展示效果
3. 然后做 **菜单/页面目录整理**，进一步统一业务域结构
4. 最后补一轮 **问题清单归档**，确保继续开发前基线清晰

## 5. 下一步操作指令（可直接复制）

### 5.1 给 Kiro 的指令模板

1. ~~`请开始 P2：先梳理当前菜单、按钮、接口，给出 RBAC 权限点设计，并在我确认后落地实现。`~~ ✅ 已完成
2. ~~`请先实现菜单级 RBAC：后端返回当前用户权限，前端根据权限动态显示菜单。`~~ ✅ 已完成
3. ~~`请继续实现按钮级 RBAC：控制新增、编辑、删除、审核、处理等按钮显示与接口访问。`~~ ✅ 已完成
4. ~~`请继续实现数据范围 RBAC：按角色或权限控制用户可查看的数据范围，并做 compile + test + build 校验。`~~ ✅ 已完成
5. `请帮我执行数据库脚本 sprint4_p2_rbac_init.sql，并验证权限数据是否正确初始化。`（需人工执行）
6. `请启动后端服务，我需要进行人工回归验收。`（需人工执行）
7. `请补充 API 文档中的权限说明，并更新部署文档。`（已完成）

### 5.2 本地执行命令（开发与验证）

1. 数据库脚本执行：
```sql
-- 在 MySQL 客户端执行
SOURCE e:/project/Personal/community-management-platform/houduan/src/main/resources/sql/sprint4_p2_rbac_init.sql;

-- 验证权限数据
SELECT role_code, COUNT(*) FROM sys_role_permission GROUP BY role_code;
SELECT permission_type, COUNT(*) FROM sys_permission WHERE status = 1 GROUP BY permission_type;
```

2. 后端编译与测试（已通过）：
```bash
cd e:/project/Personal/community-management-platform/houduan
mvn -DskipTests -Dmaven.resources.skip=true compile
mvn test
```

3. 前端构建（已通过）：
```bash
cd e:/project/Personal/community-management-platform/qianduan
npm.cmd run build
```

4. 联调启动：
```bash
cd e:/project/Personal/community-management-platform/houduan
mvn spring-boot:run
```

5. 权限回归重点：
- 管理员与普通用户菜单差异
- 关键操作按钮显示差异
- 列表接口数据范围差异
- 越权访问接口时的拒绝结果

## 6. 会话续接摘要（给下一个模型）

当前项目已完成 Sprint-1、Sprint-2、P1 与 P2 的代码实现，包括：
- 积分系统、消息中心、工单超时管理
- 服务评价闭环、自动派单规则
- RBAC 权限模型（菜单权限、按钮权限、数据范围权限）
- 前后端权限接入与风险修复
- 后端 compile、test、前端 build 验证通过

**当前状态**：代码实现完成，编译/测试/构建已通过，SQL 脚本已编写。

**下一步**：
1. 执行数据库脚本 `sprint4_p2_rbac_init.sql`
2. 启动服务进行人工回归验收
3. 补充 API 文档与部署文档

**注意**：不要重复实现已完成的功能，不要回退现有代码。

## 7. 本次会话操作记录（2026-03-08）

### 7.1 已完成代码改动
- [x] 更新 `API_DOC.md`：补充并重写了积分中心、消息中心、工单 Sprint-2 字段、统计接口新增字段等说明。
- [x] Sprint-2 工单字段改造（后端）：
  - `WorkOrder` 实体新增：`assigneeId`、`deadline`、`isOvertime`
  - `WorkOrderService` 新增筛选参数支持（责任人/超时/截止时间区间）
  - `WorkOrderMapper` 新增超时相关 SQL（超时计数、今日待办计数、批量标记超时）
  - `WorkOrderController` 接入新查询参数与处理逻辑
- [x] 统计接口接入超时维度：`StatisticsController` 增加超时与今日待办统计字段。
- [x] 定时任务改造：`ScheduledTasks` 增加“每小时扫描超时工单”任务。
- [x] SQL 脚本新增：`houduan/src/main/resources/sql/sprint2_workorder_overtime.sql`
- [x] `init.sql` 末尾追加 Sprint-2 工单字段 `ALTER TABLE` 语句。
- [x] 最小单元测试已新增（未执行）：
  - `PointsAccountServiceTest`
  - `MessageNoticeServiceTest`
- [x] 消息中心中文文案补充：`MessageNoticeController` 中“消息不存在或无权限操作”。

### 7.2 当前已知问题/风险
- [x] `PointsAccountService` 历史乱码字符串已修复（`IllegalArgumentException` 文案）。
- [x] 已执行并通过编译与测试（见 7.4）。
- [ ] `mvn test` 依赖下载与本地 Maven 仓库写权限在沙箱下可能失败，必要时需非沙箱执行。

### 7.3 下次会话建议第一条指令（可直接复制）
`请按扩展规划继续实现前端联调：先接入积分中心和消息中心页面，再把首页看板接入超时预警与今日待办。`

### 7.4 本次会话验证结果（2026-03-08）
- [x] `mvn -DskipTests -Dmaven.resources.skip=true compile` 通过。
- [x] `mvn test` 通过（11 tests, 0 failures, 0 errors）。
- [x] 修复测试文件编码问题（`PointsAccountServiceTest`、`MessageNoticeServiceTest`）。

### 7.5 本次会话新增完成项（2026-03-08）
- [x] 统一以下接口中文返回文案：
  - `ActivityRegistrationController`
  - `VolunteerController`
  - `MessageNoticeController`
- [x] 修复 `MessageNoticeController` 中历史乱码提示文本。
- [x] 完成改动后的编译与测试回归：
  - `mvn -DskipTests -Dmaven.resources.skip=true compile`
  - `mvn test`（11 tests, 0 failures）

## 8. 下次会话执行清单（建议）

1. 在数据库执行 Sprint-1/Sprint-2 增量脚本并回归：
```sql
SOURCE e:/zsy/biyesheji/biyesheji/houduan/src/main/resources/sql/sprint1_points.sql;
SOURCE e:/zsy/biyesheji/biyesheji/houduan/src/main/resources/sql/sprint1_message.sql;
SOURCE e:/zsy/biyesheji/biyesheji/houduan/src/main/resources/sql/sprint2_workorder_overtime.sql;
```
2. 执行并通过：
   - `cd e:/zsy/biyesheji/biyesheji/houduan`
   - `mvn -DskipTests -Dmaven.resources.skip=true compile`
   - `mvn test`
3. 回归验证工单新能力：
   - `GET /workorder/list?isOvertime=1`
   - `GET /workorder/list?deadlineStart=2026-03-01&deadlineEnd=2026-03-31`
   - `GET /statistics/overview`
   - `GET /statistics/workorder`
4. 前端页面接入（积分中心、消息中心、工单超时看板）。

## 9. 本轮联调更新（2026-03-08）

### 9.1 本轮已完成
- [x] 按清单执行后端校验：
  - `cd e:/zsy/biyesheji/biyesheji/houduan`
  - `mvn "-DskipTests" "-Dmaven.resources.skip=true" compile` 通过
  - `mvn test` 通过（11 tests, 0 failures, 0 errors）
- [x] 前端接入积分中心页面（含我的积分、积分明细、排行榜）：
  - `GET /points/my`
  - `GET /points/records`
  - `GET /points/rank`
- [x] 前端接入消息中心页面（含未读数、单条已读、全部已读、管理员列表）：
  - `GET /message/my`
  - `GET /message/unread/count`
  - `PUT /message/{id}/read`
  - `PUT /message/read-all`
  - `GET /message/admin/list`
- [x] 前端接入工单超时看板页面（含超时与今日待办数据）：
  - `GET /statistics/overview`
  - `GET /statistics/workorder`
- [x] 路由与菜单已接入：积分中心、消息中心、工单超时看板。
- [x] 前端构建校验通过：`npm.cmd run build`。

### 9.2 本轮未完成
- [ ] 尚未在真实数据库执行并回归 SQL：
  - `sprint1_points.sql`
  - `sprint1_message.sql`
  - `sprint2_workorder_overtime.sql`
- [ ] 尚未完成浏览器端人工回归（仅完成编译/构建验证）。

### 9.3 下一步指令（下轮可直接执行）
1. 启动后端并带真实数据回归：
   - `cd e:/zsy/biyesheji/biyesheji/houduan`
   - `mvn spring-boot:run`
2. 执行数据库增量脚本并回归接口。
3. 前端逐页验收：`/points`、`/message`、`/workorder-overtime`。
4. 如需增强：补消息中心“查看详情弹窗”和看板图表可视化。

## 10. 本轮首页看板补充（2026-03-08）

### 10.1 已完成
- [x] 完成 Sprint-2 剩余项：首页接入“超时预警 + 今日待办”。
- [x] 首页统计卡新增并显示：
  - `overtimeOrderCount`（超时未结工单）
  - `todayTodoOrderCount`（今日待办工单）
- [x] 首页统计数据联动接口：
  - `GET /statistics/overview`
  - `GET /statistics/workorder`
  - `GET /statistics/activity`
- [x] 本轮前端构建校验通过：`npm.cmd run build`。

### 10.2 当前剩余待办（按优先级）
1. 在真实数据库执行并回归增量 SQL：
   - `sprint1_points.sql`
   - `sprint1_message.sql`
   - `sprint2_workorder_overtime.sql`
2. 执行端到端回归验收（登录后）：
   - 积分中心：`/points`
   - 消息中心：`/message`
   - 工单超时看板：`/workorder-overtime`
   - 首页看板：`/home`

### 10.3 下一步指令（下轮可直接执行）
`请按扩展规划继续：先完成真实数据库增量脚本回归，再执行首页/积分/消息/工单超时看板的端到端验收，并输出问题清单与修复建议。`

## 11. P1 实施进展（2026-03-08）

### 11.1 本轮已完成
- [x] 满意度评价闭环（后端 + 前端）：
  - 新增服务评价表：`service_evaluation`
  - 新增评价接口：
    - `POST /evaluation/workorder/{workOrderId}`（提交/更新评价）
    - `GET /evaluation/workorder/{workOrderId}`（查询我的评价）
    - `GET /evaluation/my`（我的评价分页）
    - `GET /evaluation/my/workorders`（我的已完成工单，含是否已评价标记）
    - `GET /evaluation/admin/list`（管理员评价列表）
    - `GET /evaluation/stats`（评价统计）
  - 前端新增“服务评价”页面并接入以上接口。
- [x] 网格化自动派单（后端 + 前端）：
  - 新增派单规则表：`grid_dispatch_rule`
  - 新增规则管理接口：
    - `GET /dispatch/rule/list`
    - `POST /dispatch/rule`
    - `PUT /dispatch/rule/{id}`
    - `DELETE /dispatch/rule/{id}`
    - `GET /dispatch/rule/preview`
  - 工单创建时接入自动派单：若命中规则自动写入 `assigneeId`，并向责任人发送站内消息。
  - 前端新增“派单规则”管理页面（管理员）。
- [x] 统计增强：`/statistics/overview` 新增返回 `evaluationStats`。
- [x] 新增增量脚本：`sprint3_p1_evaluation_dispatch.sql`。

### 11.2 本轮验证结果
- [x] `mvn "-DskipTests" "-Dmaven.resources.skip=true" compile` 通过。
- [x] `mvn test` 通过（11 tests, 0 failures, 0 errors）。
- [x] `npm.cmd run build` 通过。

### 11.3 下一步（建议）
1. 在真实数据库执行：
   - `SOURCE e:/zsy/biyesheji/biyesheji/houduan/src/main/resources/sql/sprint3_p1_evaluation_dispatch.sql;`
2. 端到端回归：
   - 用户提交工单 -> 自动匹配责任人 -> 责任人收到消息
   - 工单完成后，用户在“服务评价”提交评价
   - 管理员查看评价统计与评价列表
3. 进入 P2：RBAC 菜单/按钮/数据范围细化。

## 12. 当前权威状态（2026-03-10）

### 12.1 已完成
- [x] Sprint-1：积分中心、消息中心、中文返回文案、测试、前端联调、真实库执行与验收
- [x] Sprint-2：工单超时字段、定时扫描、超时看板、首页“超时预警 + 今日待办”
- [x] P1：服务评价闭环、自动派单规则、评价统计、派单规则管理页面
- [x] 菜单结构整理：新增一级分组“服务成长”“工单治理”

### 12.2 未完成
- [ ] 当前无阻塞性未完成项，后续可直接进入展示优化或新功能扩展

### 12.3 说明
- 本文档第 7 至 11 节主要保留为历史会话记录。
- 如历史记录中的“下一步”与当前状态不一致，以本节和第 1 至 6 节为准。


## 13. P2 代码实现完成总结（2026-03-11）

### 13.1 已完成的 P2 代码实现
- [x] 权限模型设计：
  - `sys_permission` 表（19 个菜单权限、52 个按钮权限、12 个数据范围权限）
  - `sys_role_permission` 表（角色权限关联）
  - 权限初始化 SQL：`sprint4_p2_rbac_init.sql`
- [x] 后端权限能力：
  - `SysPermission` 实体类
  - `SysPermissionService` 服务类（含缓存）
  - `@Auth` 注解扩展（支持权限点校验）
  - `AuthAspect` 改造（统一角色与权限点判断）
  - `/user/info` 返回权限列表
- [x] 前端权限能力：
  - `user store` 增加权限列表缓存
  - `hasPerm()` 和 `hasAnyPerm()` 方法
  - 登录后拉取权限信息
  - 路由守卫权限校验
  - Layout 菜单动态显示
- [x] 按钮权限接入：52 个按钮权限点已接入前端页面
- [x] 数据范围权限收口：工单、预约、消息、评价、志愿者、公告
- [x] 风险修复：导出接口鉴权、前端越权按钮显示
- [x] 验证通过：后端 compile、test、前端 build

### 13.2 待执行项
- [ ] 数据库脚本执行：`sprint4_p2_rbac_init.sql`
- [ ] 人工回归验收（需真实环境）
- [x] API 文档更新
- [x] 部署文档更新

### 13.3 下一步指令
`请在数据库执行 sprint4_p2_rbac_init.sql 脚本，然后启动服务进行人工回归验收。`

## 14. 本轮收口完成（2026-03-19）

- [x] 工单治理前端收口：
  - 工单列表新增超时状态筛选、截止日期筛选、责任人筛选（管理员）
  - 工单列表新增责任人、截止时间、超时状态展示
  - 工单新增/编辑/处理弹窗补充截止时间与责任人字段
- [x] 用户管理前后端一致性修复：
  - 修复“前端可编辑角色、后端实际不保存角色”的问题
  - 新增用户与编辑用户补充手机号唯一性与角色有效性校验
- [x] 自动派单规则预览前端接入：
  - 派单规则页面新增“规则预览”入口
  - 接入 `/dispatch/rule/preview` 接口
- [x] 公告页面收口：
  - 新增公告统计弹窗，接入 `/notice/{id}/stats`
  - 新增通知订阅设置弹窗
- [x] 公告订阅推送闭环：
  - 新增 `NoticeSubscriptionController`
  - 完成订阅配置读取与保存
  - 公告发布时按订阅配置发送站内消息
- [x] 导出入口补齐：
  - 活动管理页面新增导出入口
  - 流动人口页面新增导出入口
- [x] 数据库总脚本补齐：
  - `community_service.sql` 已整合结构、演示数据、索引、RBAC 初始化数据，可直接重建项目库

## 15. 本轮继续完善（2026-03-20）

- [x] 消息中心体验增强：
  - 新增“消息详情”弹窗
  - 我的消息与管理员消息列表均可查看完整内容
- [x] 统计看板展示增强：
  - 首页新增服务评价卡片、评价分布图、治理风险聚焦图
  - 工单超时看板升级为图表化展示，并增加动态治理提示
- [x] API 文档同步：
  - `API_DOC.md` 已补充权限说明、AI 接口、评价、派单规则、公告订阅、导出等内容
- [x] 部署与环境文档同步：
  - `README.md` 已切换为以 `community_service.sql` 作为推荐初始化脚本
  - `环境配置指南.md` 已同步总脚本初始化方式与默认账号说明

## 16. 真实环境回归记录（2026-03-21）

### 16.1 本轮已确认通过
- [x] 数据库总脚本最新版已正确落库：
  - `notice_subscription` 表存在
  - `service_evaluation`、`message_notice`、`points_account`、`points_record`、`grid_dispatch_rule` 已有演示数据
  - `work_order.deadline` 演示数据与超时数据已落库
- [x] 管理员与普通用户登录正常
- [x] 菜单权限差异正常，普通用户访问管理员页面会被拦回
- [x] 首页统计、工单超时看板、公告统计可正常展示
- [x] 普通用户“我的工单”“待评价工单”“我的评价”“消息中心”“积分中心”主链路已恢复正常
- [x] 公告订阅读取接口与保存接口已通过接口层验证
- [x] 分页总数问题已修复：`MybatisPlusConfig` 已接入分页拦截器

### 16.2 本轮发现并已修复
- [x] 修复数据库总脚本中多张表残留的 `AUTO_INCREMENT` 起始值问题，避免演示数据外键错位
- [x] 修复 `community_service.sql` 中工单超时演示更新按旧 `id` 命中的问题，改为按标题命中
- [x] 修复 `SysUserService` 使用 Redis 旧缓存导致重建数据库后登录仍拿到旧用户 `id` 的问题

### 16.3 当前剩余问题/建议
- [x] 普通用户工单列表中的责任人名称展示问题已修复
- [ ] 如需最终交付展示，可继续补更多“新增数据 -> 多模块联动”的演示脚本

### 16.4 下一步建议第一条指令
`请基于扩展规划当前权威状态，直接继续做最终展示优化：整理答辩演示路径、关键页面截图清单、系统亮点话术和剩余可扩展方向。`

## 17. 本轮继续收口（2026-03-21）

- [x] 修复普通用户工单列表责任人名称展示问题：
  - `WorkOrder` 新增 `assigneeName` 展示字段
  - `WorkOrderController` 在列表与详情接口中补充责任人姓名回填
- [x] 路由配置已按业务域重构整理：
  - `router/index.js` 中按公共、居民、社区事务、服务成长、工单治理、治安管理、系统管理分组维护
- [x] 真实环境回归进一步确认：
  - 登录、权限、统计看板、公告统计、消息详情、积分排行榜、服务评价链路均已可用
- [x] 当前最后待验证项已完成：
  - 重启后端后已复测普通用户工单列表责任人名称恢复为真实姓名
  - 已完成“新增公告 -> 订阅推送 -> 用户消息查看”的端到端人工回归

## 18. 第四轮真实环境回归（2026-03-22）

### 18.1 已确认通过
- [x] 数据库主键与演示数据关系已恢复正常：
  - `sys_user` 主键从 `1` 开始
  - `work_order`、`service_evaluation`、`message_notice`、`points_account` 的关联关系正确
- [x] 普通用户登录后 `/workorder/list` 可正常返回本人数据
- [x] 普通用户工单列表责任人名称已可展示真实姓名
- [x] `/evaluation/my/workorders` 与 `/evaluation/my` 已恢复正常
- [x] `/notice/list` 分页总数已恢复正常
- [x] 公告订阅读取与保存接口均已通过接口层验证
- [x] 工单超时看板、首页统计、消息中心详情、积分排行榜均已通过真实环境回归

### 18.2 本轮额外修复
- [x] 移除 `SysUserService` 中登录依赖的 Redis 用户缓存，避免重建数据库后仍返回旧用户 `id`
- [x] 数据库总脚本统一修正 `AUTO_INCREMENT` 起始值
- [x] 数据库总脚本修正工单超时演示更新语句的命中方式

### 18.3 当前剩余待办
- [x] 做一轮“新增公告 -> 订阅推送 -> 用户消息查看”的完整端到端人工回归
- [ ] 如需继续优化，可继续补答辩展示脚本、截图清单与说明话术

### 18.4 下一步建议第一条指令
`请基于扩展规划当前权威状态，直接继续做最终展示优化：整理答辩演示路径、关键页面截图清单、系统亮点话术和剩余可扩展方向。`

## 19. 最终端到端验收结果（2026-03-22）

### 19.1 本轮已确认通过
- [x] 管理员新增 `NOTICE` 类型公告成功
- [x] 普通用户公告订阅配置生效
- [x] 新公告发布后，普通用户消息中心成功收到“您订阅的通知已发布”站内消息
- [x] 消息详情弹窗可查看推送详情，并支持在详情中标记已读
- [x] 普通用户“工单 -> 评价 -> 消息 -> 积分 -> 公告订阅推送”链路已通过真实环境回归

### 19.2 当前权威结论
- [x] 数据库总脚本 `community_service.sql` 可用于当前项目完整重建
- [x] 前后端主要业务功能已完成并通过多轮真实环境回归
- [x] 当前项目已可进入“最终展示优化 / 毕设答辩准备 / 新功能扩展”阶段

### 19.3 后续建议
- [ ] 如需继续开发，优先做“答辩展示脚本 + 截图清单 + 亮点说明”
- [ ] 如需继续扩展，可进入对象存储、Docker、小程序端、多通道通知等方向

### 19.4 下一位模型建议第一条指令
`请直接基于扩展规划第 19 节继续：先整理一份适合毕业设计答辩的演示脚本、页面展示顺序、功能亮点总结和截图清单，再按需提出最后一轮优化建议。`
